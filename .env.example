# .env.example
# Environment variables for database connection and Docker Compose

# PostgreSQL Connection Details (used by docker-compose and importer.py)
POSTGRES_DB=ans_data
POSTGRES_USER=ans_user
POSTGRES_PASSWORD=changeme_strong_password # Use a strong password!

# Database Host and Port Configuration
DB_HOST=db                 # Service name defined in docker-compose.yml
DB_PORT_HOST=5433          # Port exposed on HOST machine
# DB_PORT_CONTAINER=5432   # Internal port inside the container (usually fixed)